# Animalese-Unity 交互测试用例文档 (Test Cases)

本文档提供了一组专门用于在 Unity 编辑器中验证发音表现、标点韵律、富文本兼容性及崩溃防御的测试文本。
可直接将文本块复制粘贴至 Example 示例场景中的 `TMP_InputField` 输入框中进行测试。

---

## 测试场景索引

- [用例 1：英文双音素与常规发音](#用例-1英文双音素与常规发音)
- [用例 2：标点符号与语调韵律（问号/感叹号/复合震惊）](#用例-2标点符号与语调韵律问号感叹号复合震惊)
- [用例 3：尾音拉长与省略号渐弱（波浪号/破折号/长音）](#用例-3尾音拉长与省略号渐弱波浪号破折号长音)
- [用例 4：Emoji 与代理对防御测试（防崩溃验证）](#用例-4emoji-与代理对防御测试防崩溃验证)
- [用例 5：TextMeshPro 富文本标签与打字机绝对同步](#用例-5textmeshpro-富文本标签与打字机绝对同步)
- [用例 6：中文与日文跨语言拟音测试](#用例-6中文与日文跨语言拟音测试)
- [用例 7：长篇复合综合剧情对话](#用例-7长篇复合综合剧情对话)

---

### 用例 1：英文双音素与常规发音
> **测试目的**：验证英文 26 字母常量池查表与 `ch`, `sh`, `th`, `wh`, `ph` 贪心双音素匹配的准确度。

```text
The white ship will choose which photo to share!
```

- **预期听感**：
  - `The` 中的 `th` 贪心合并发一个音素；
  - `white` 中的 `wh` 发一个音素；
  - `ship` 中的 `sh` 发一个音素；
  - `choose` 中的 `ch` 发一个音素；
  - `photo` 中的 `ph` 发一个音素；
  - 节奏欢快、流畅，无破音。
- **预期视觉**：打字机平滑逐字推进至句末。

---

### 用例 2：标点符号与语调韵律（问号/感叹号/复合震惊）
> **测试目的**：验证单问号升调、感叹号爆发音量、以及 `?!` / `!?` 复合震惊语调。

```text
Are you sure? Look out! What on earth is that?!
```

- **预期听感**：
  - `sure?`：句子末尾音素音调明显上扬；
  - `Look out!`：感叹号前音素音调拔高且音量显著增大（1.35x）；
  - `is that?!`：复合震惊标点，音调大幅飙升（+0.45）且音量爆发（1.4x），有明显的惊讶感；
  - 标点处有符合语流的停顿。
- **预期视觉**：遇到标点处有停顿节奏感。

---

### 用例 3：尾音拉长与省略号渐弱（波浪号/破折号/长音）
> **测试目的**：验证 `-` / `—` / `ー` 延长前序音素时长，`~` 悠闲音调上扬，以及 `...` 的音量渐弱。

```text
Wait— don't go yet~ I... I feel so sleepy...
```

- **预期听感**：
  - `Wait—`：`Wait` 最后一个音素播放时长显著延长（延音效果）；
  - `yet~`：尾音时长延长的同时伴随音高轻微上浮，呈现撒娇/悠闲感；
  - `I...` 与 `sleepy...`：在省略号出现前，音量逐渐弱化至 0.6x~0.8x，音调微降，并产生长停顿（Pause 5.0f）。
- **预期视觉**：波浪号与破折号停顿短暂，省略号有明显的静止停顿感。

---

### 用例 4：Emoji 与代理对防御测试（防崩溃验证）
> **测试目的**：验证 UTF-16 代理对（Surrogate Pair）修复情况。确保 Emoji 不崩溃、不发出奇怪噪音，且打字机完整渲染出 Emoji。

```text
Hello neighbor! 🐱 Look at this cool prize! 🎁🎉 Let's party! ✨
```

- **预期听感**：
  - 遇到 🐱、🎁、🎉、✨ 等 Emoji 时，**完全不发出任何音频爆音或拟音**，作为纯静音微小停顿处理；
  - **控制台绝对不会抛出** `ArgumentException: The string contains an invalid surrogate pair.`。
- **预期视觉**：
  - 打字机完整且平滑地打出 Emoji 符号，没有乱码和字符吞截。

---

### 用例 5：TextMeshPro 富文本标签与打字机绝对同步
> **测试目的**：验证 TextMeshPro 原生标签解析与打字机 `maxVisibleCharacters` 的 1:1 绝对对齐，确保标签内容不被读出。

```text
Welcome to <color=#FFD700><b>Animal Crossing</b></color>! Enjoy your <size=130%><color=#00FF7F>island</color></size> life.
```

- **预期听感**：
  - **绝不朗读** `<color>`、`#FFD700`、`<b>`、`<size>` 等富文本标签里的任何字母；
  - 仅依次发音：`Welcome to Animal Crossing! Enjoy your island life.`；
- **预期视觉**：
  - 打字过程中**绝不提前暴露未闭合的标签代码**；
  - “Animal Crossing” 以金黄色加粗直接逐步显现；
  - “island” 放大绿色平滑逐步显现；
  - 打字机与声音完全严丝合缝对齐，在读完最后一个字母时打字机恰好全部打完。

---

### 用例 6：中文与日文跨语言拟音测试
> **测试目的**：验证非英文字符的 Unicode 散列回退拟音及 0 GC 表现。

```text
你好！今天的天气真不错呢~ こんにちは！素敵な一日ですね。
```

- **预期听感**：
  - 每个汉字和假名都根据自身字符码计算固定确定的音素片段，发音规律、韵律感强；
  - 句末 `~` 和句号正常停顿与延音。
- **性能预期**：运行期间 Profiler 中 GC Alloc 为 0。

---

### 用例 7：长篇复合综合剧情对话
> **测试目的**：用于测试长文本性能、连贯性以及倍速快进播放时的体验。

```text
<color=#1E90FF><b>Tom Nook</b></color>: "Yes, yes! Welcome to your new <color=#32CD32>island</color>!
Did you find your tent yet? ⛺
No?! Don't worry~ Here is your bill... <b>98,000 Bells</b>, please!" 💰
```

- **复合特性验证清单**：
  - [x] 富文本标签穿透与着色展示
  - [x] 英文常规词朗读
  - [x] 问号结尾升调 (`yet?`)
  - [x] 帐篷 Emoji ⛺ 静音展示
  - [x] 震惊标点爆音 (`No?!`)
  - [x] 波浪号延音 (`worry~`)
  - [x] 省略号渐弱停顿 (`bill...`)
  - [x] 数字与钱袋 Emoji 💰 顺利展示与收尾

